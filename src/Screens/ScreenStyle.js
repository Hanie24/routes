import { StyleSheet } from "react-native";

const stylesScreen = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });

export {stylesScreen}