import { StyleSheet } from "react-native";

const styleButton = StyleSheet.create({
    button: {
      backgroundColor: '#000',
      minWidth: 200,
      padding: 10,
      marginTop: 10
    },
    text: {
      fontWeight: 'bold',
      fontSize: 18,
      color: '#f1f1f1', 
      textAlign: 'center'
    },
});

export {styleButton}